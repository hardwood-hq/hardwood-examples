#
#  SPDX-License-Identifier: Apache-2.0
#
#  Copyright The original authors
#
#  Licensed under the Apache Software License version 2.0, available at http://www.apache.org/licenses/LICENSE-2.0
#

# Run in a container:  docker compose run --rm --build typed-accessors
# The sample Parquet files are bundled on the classpath, so there is no data volume to mount.
services:
  typed-accessors:
    build:
      context: .
