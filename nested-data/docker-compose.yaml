#
#  SPDX-License-Identifier: Apache-2.0
#
#  Copyright The original authors
#
#  Licensed under the Apache Software License version 2.0, available at http://www.apache.org/licenses/LICENSE-2.0
#

# Run in a container:  docker compose run --rm --build nested-data
# The sample files ship inside the image and are read straight from the classpath — no volumes.
services:
  nested-data:
    build:
      context: .
